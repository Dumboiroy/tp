package seedu.address.model;

/**
 * Represents the current view mode of the application.
 * Used to determine whether the UI is displaying persons or appointments.
 */
public enum ViewMode {
    PERSONS,
    APPOINTMENTS
}
